# this directory implements these libraries
lib-y := libsys.a libdriver.a libtimers.a libnucc.a \
	 libcurses.a libutil.a

libsys.a-obj-y := syslib/ sysutil/
libdriver.a-obj-y := libdriver/
libtimers.a-obj-y := timers/
libnucc.a-obj-y := ansi/ editline/ gnu/ gnu/ ip/ math/ math64/ other/ \
		   posix/ regex/ stdio/ stdtime/ sysvipc/
libcurses.a-obj-y := curses/
libutil.a-obj-y := util/
